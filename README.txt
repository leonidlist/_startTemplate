StartTemplate INSTALLATION STEPS:

1.Initiate package.json

	npm install

2.Create file .bowerrc with following content

	{
		"directory" : "app/libs"
	}

3.Install jQuery library via Bower

	bower i --save-dev jquery
